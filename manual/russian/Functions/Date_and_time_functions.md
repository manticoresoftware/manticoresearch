# Функции даты и времени

Обратите внимание, что `CURTIME()`, `UTC_TIME()`, `UTC_TIMESTAMP()` и `TIMEDIFF()` могут быть приведены к числовым типам с помощью произвольных функций преобразования, таких как `BIGINT()`, `DOUBLE()` и т.д.

### NOW()
<!-- example NOW -->
Возвращает текущую метку времени в виде INTEGER.

<!-- request SQL -->
```sql
select NOW();
```
<!-- response SQL -->
```sql
+------------+
| NOW()      |
+------------+
| 1615788407 |
+------------+
```
<!-- end -->

### CURTIME()
<!-- example CURTIME -->
Возвращает текущее время в локальном часовом поясе в формате `hh:ii:ss`.

<!-- request SQL -->
```sql
select CURTIME();
```
<!-- response SQL -->
```sql
+-----------+
| CURTIME() |
+-----------+
| 07:06:30  |
+-----------+
```
<!-- end -->

### CURDATE()
<!-- example CURDATE -->
Возвращает текущую дату в локальном часовом поясе в формате `YYYY-MM-DD`.

<!-- request SQL -->
```sql
select curdate();
```
<!-- response SQL -->
```sql
+------------+
| curdate()  |
+------------+
| 2023-08-02 |
+------------+
```
<!-- end -->

### UTC_TIME()
<!-- example UTC_TIME -->
Возвращает текущее время в часовом поясе UTC в формате `hh:ii:ss`.

<!-- request SQL -->
```sql
select UTC_TIME();
```
<!-- response SQL -->
```sql
+------------+
| UTC_TIME() |
+------------+
| 06:06:18   |
+------------+
```
<!-- end -->

### UTC_TIMESTAMP()
<!-- example UTC_TIMESTAMP -->
Возвращает текущее время в часовом поясе UTC в формате `YYYY-MM-DD hh:ii:ss`.

<!-- request SQL -->
```sql
select UTC_TIMESTAMP();
```
<!-- response SQL -->
```sql
+---------------------+
| UTC_TIMESTAMP()     |
+---------------------+
| 2021-03-15 06:06:03 |
+---------------------+
```
<!-- end -->

### SECOND()
<!-- example SECOND -->
Возвращает целочисленное значение секунд (в диапазоне 0..59) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select second(now());
```
<!-- response SQL -->
```sql
+---------------+
| second(now()) |
+---------------+
| 52            |
+---------------+
```
<!-- end -->

### MINUTE()
<!-- example MINUTE -->
Возвращает целочисленное значение минут (в диапазоне 0..59) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select minute(now());
```
<!-- response SQL -->
```sql
+---------------+
| minute(now()) |
+---------------+
| 5             |
+---------------+
```
<!-- end -->

### HOUR()
<!-- example HOUR -->
Возвращает целочисленное значение часов (в диапазоне 0..23) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select hour(now());
```
<!-- response SQL -->
```sql
+-------------+
| hour(now()) |
+-------------+
| 7           |
+-------------+
```
<!-- end -->

### DAY()
<!-- example DAY -->
Возвращает целочисленное значение дня месяца (в диапазоне 1..31) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select day(now());
```
<!-- response SQL -->
```sql
+------------+
| day(now()) |
+------------+
| 15         |
+------------+
```
<!-- end -->

### MONTH()
<!-- example MONTH -->
Возвращает целочисленное значение месяца (в диапазоне 1..12) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select month(now());
```
<!-- response SQL -->
```sql
+--------------+
| month(now()) |
+--------------+
| 3            |
+--------------+
```
<!-- end -->

### QUARTER()
<!-- example QUARTER -->
Возвращает целочисленное значение квартала года (в диапазоне 1..4) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select quarter(now());
```
<!-- response SQL -->
```sql
+----------------+
| quarter(now()) |
+----------------+
| 2              |
+----------------+
```
<!-- end -->

### YEAR()
<!-- example YEAR -->
Возвращает целочисленное значение года (в диапазоне 1969..2038) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select year(now());
```
<!-- response SQL -->
```sql
+-------------+
| year(now()) |
+-------------+
| 2024        |
+-------------+
```
<!-- end -->

### DAYNAME()
<!-- example DAYNAME -->
Возвращает название дня недели для заданного аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select dayname(now());
```
<!-- response SQL -->
```sql
+----------------+
| dayname(now()) |
+----------------+
| Wednesday      |
+----------------+
```
<!-- end -->

### MONTHNAME()
<!-- example MONTHNAME -->
Возвращает название месяца для заданного аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select monthname(now());
```
<!-- response SQL -->
```sql
+------------------+
| monthname(now()) |
+------------------+
| August           |
+------------------+
```
<!-- end -->

### DAYOFWEEK()
<!-- example DAYOFWEEK -->
Возвращает целочисленное значение индекса дня недели (в диапазоне 1..7) для заданного аргумента метки времени с учётом текущего часового пояса.
Обратите внимание, что неделя начинается с воскресенья.

<!-- request SQL -->
```sql
select dayofweek(now());
```
<!-- response SQL -->
```sql
+------------------+
| dayofweek(now()) |
+------------------+
| 5                |
+------------------+
```
<!-- end -->

### DAYOFYEAR()
<!-- example DAYOFYEAR -->
Возвращает целочисленное значение дня года (в диапазоне 1..366) для заданного аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select dayofyear(now());
```
<!-- response SQL -->
```sql
+------------------+
| dayofyear(now()) |
+------------------+
|              214 |
+------------------+
```
<!-- end -->

### YEARWEEK()
<!-- example YEARWEEK -->
Возвращает целочисленное значение года и кода дня первого дня текущей недели (в диапазоне 1969001..2038366) для заданного аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select yearweek(now());
```
<!-- response SQL -->
```sql
+-----------------+
| yearweek(now()) |
+-----------------+
|         2023211 |
+-----------------+
```
<!-- end -->

### YEARMONTH()
<!-- example YEARMONTH -->
Возвращает целочисленное значение года и кода месяца (в диапазоне 196912..203801) из аргумента метки времени с учётом текущего часового пояса.

<!-- request SQL -->
```sql
select yearmonth(now());
```
<!-- response SQL -->
```sql
+------------------+
| yearmonth(now()) |
+------------------+
| 202103           |
+------------------+
```
<!-- end -->

### YEARMONTHDAY()
<!-- example YEARMONTHDAY -->
Возвращает целочисленное значение года, месяца и дня (в диапазоне от 19691231 до 20380119) на основе текущего часового пояса.

<!-- request SQL -->
```sql
select yearmonthday(now());
```
<!-- response SQL -->
```sql
+---------------------+
| yearmonthday(now()) |
+---------------------+
| 20210315            |
+---------------------+
```
<!-- end -->

### TIMEDIFF()
<!-- example TIMEDIFF -->
Вычисляет разницу между двумя метками времени в формате `hh:ii:ss`.

<!-- request SQL -->
```sql
select timediff(1615787586, 1613787583);
```
<!-- response SQL -->
```sql
+----------------------------------+
| timediff(1615787586, 1613787583) |
+----------------------------------+
| 555:33:23                        |
+----------------------------------+
```
<!-- end -->

### DATEDIFF()
<!-- example DATEDIFF -->
Вычисляет количество дней между двумя заданными метками времени.

<!-- request SQL -->
```sql
select datediff(1615787586, 1613787583);
```
<!-- response SQL -->
```sql
+----------------------------------+
| datediff(1615787586, 1613787583) |
+----------------------------------+
|                               23 |
+----------------------------------+
```
<!-- end -->

### DATE()
<!-- example DATE -->
Форматирует часть даты из аргумента метки времени в виде строки в формате `YYYY-MM-DD`.

<!-- request SQL -->
```sql
select date(now());
```
<!-- response SQL -->
```sql
+-------------+
| date(now()) |
+-------------+
| 2023-08-02  |
+-------------+
```
<!-- end -->

### TIME()
<!-- example TIME -->
Форматирует часть времени из аргумента метки времени в виде строки в формате `HH:MM:SS`.

<!-- request SQL -->
```sql
select time(now());
```
<!-- response SQL -->
```sql
+-------------+
| time(now()) |
+-------------+
| 15:21:27    |
+-------------+
```
<!-- end -->

### DATE_FORMAT()
<!-- example DATE_FORMAT -->
Возвращает отформатированную строку на основе переданных аргументов даты и формата. Аргумент формата использует те же спецификаторы, что и функция [strftime](https://man7.org/linux/man-pages/man3/strftime.3.html). Для удобства приведены некоторые распространённые спецификаторы формата:

- `%Y` - четырёхзначный год
- `%m` - двузначный месяц (01-12)
- `%d` - двузначный день месяца (01-31)
- `%H` - двузначный час (00-23)
- `%M` - двузначная минута (00-59)
- `%S` - двузначная секунда (00-59)
- `%T` - время в 24-часовом формате (`%H:%M:%S`)

Обратите внимание, что это не полный список спецификаторов. Пожалуйста, обратитесь к документации по `strftime()` для вашей операционной системы, чтобы получить полный список.

<!-- request SQL -->
```sql
SELECT DATE_FORMAT(NOW(), 'year %Y and time %T');
```
<!-- response SQL -->
```sql
+------------------------------------------+
| DATE_FORMAT(NOW(), 'year %Y and time %T') |
+------------------------------------------+
| year 2023 and time 11:54:52              |
+------------------------------------------+
```
<!-- end -->

В этом примере форматируется текущая дата и время, отображается четырёхзначный год и время в 24-часовом формате.

### DATE_HISTOGRAM()
<!-- example DATE_HISTOGRAM -->
`DATE_HISTOGRAM(expr, {calendar_interval='unit_name'})` принимает размер интервала в виде имени единицы и возвращает номер корзины для значения. Значения округляются до ближайшей корзины. Ключевая функция:
```sql
key_of_the_bucket = interval * floor ( value / interval )
```
Интервалы могут быть указаны с помощью имени единицы, например, `week`, или в виде единственной единицы, такой как `1M`. Однако несколько единиц, например `2d`, не поддерживаются с `calendar_interval`, но разрешены с `fixed_interval`.

Допустимые интервалы для `calendar_interval`:

- `minute`, `1m`
- `hour`, `1h`
- `day`, `1d`
- `week`, `1w` (неделя — это интервал между началом дня недели, часа, минуты, секунды и следующей недели, но в тот же день и время недели)
- `month`, `1M`
- `year`, `1y` (год — это интервал между началом дня месяца, времени и следующим годом, но в тот же день месяца и время)

Допустимые интервалы для `fixed_interval`:

- `minute`, `2m`
- `hour`, `3h`
- `day`, `5d`

Используется в агрегациях, `FACET` и группировках.

Пример:

```sql
SELECT COUNT(*),
DATE_HISTOGRAM(tm, {calendar_interval='month'}) AS months
FROM facets
GROUP BY months ORDER BY months ASC;
```

### DATE_RANGE()
<!-- example DATE_RANGE -->
`DATE_RANGE(expr, {range_from='date_math', range_to='date_math'})` принимает набор диапазонов и возвращает номер корзины для значения.
Выражение включает значение `range_from` и исключает значение `range_to` для каждого диапазона. Диапазон может быть открытым — иметь только значение `range_from` или только `range_to`.
Отличие этого от функции [RANGE()](../Functions/Arrays_and_conditions_functions.md#RANGE%28%29) заключается в том, что значения `range_from` и `range_to` могут быть выражены в выражениях [Date math](../Functions/Date_and_time_functions.md#Date-math).

Используется в агрегациях, `FACET` и группировках.

Пример:

```sql
SELECT COUNT(*),
DATE_RANGE(tm, {range_to='2017||+2M/M'},{range_from='2017||+2M/M',range_to='2017||+5M/M'},{range_from='2017||+5M/M'}) AS points
FROM idx_dates
GROUP BY points ORDER BY points ASC;
```

##### Date math

Date math позволяет работать с датами и временем непосредственно в ваших поисковых запросах. Это особенно полезно для обработки данных, которые изменяются со временем. С помощью Date math вы можете легко находить записи за определённый период, анализировать тенденции данных или управлять моментами удаления информации. Это упрощает работу с датами, позволяя добавлять или вычитать время от заданной даты, округлять даты до ближайшей единицы времени и многое другое, всё внутри ваших поисковых запросов.

Чтобы использовать Date math, начните с базовой даты, которая может быть:
- `now` для текущей даты и времени,
- или конкретной строкой даты, заканчивающейся на `||`.

Затем вы можете модифицировать эту дату с помощью операций, таких как:
- `+1y` чтобы добавить один год,
- `-1h` чтобы вычесть один час,
- `/m` чтобы округлить до ближайшего месяца.

Вы можете использовать следующие единицы в своих операциях:
- `s` для секунд,
- `m` для минут,
- `h` (или `H`) для часов,
- `d` для дней,
- `w` для недель,
- `M` для месяцев,
- `y` для лет.

Вот несколько примеров того, как можно использовать Date math:
- `now+4h` означает четыре часа от текущего времени.
- `now-2d/d` — время два дня назад, округлённое до ближайшего дня.
- `2010-04-20||+2M/d` — 20 июня 2010 года, округлённая до ближайшего дня.

<!-- proofread -->

