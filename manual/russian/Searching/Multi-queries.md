# Мультизапросы

Мультизапросы, или пакеты запросов, позволяют отправлять несколько поисковых запросов в Manticore одним сетевым запросом.

👍 Зачем использовать мультизапросы?

Основная причина — производительность. Отправляя запросы в Manticore пакетами, а не по одному, вы экономите время, снижая количество сетевых обменов. Кроме того, отправка запросов в пакете позволяет Manticore выполнять некоторые внутренние оптимизации. Если пакетные оптимизации невозможны, запросы обрабатываются по отдельности.

⛔ Когда не использовать мультизапросы?

Мультизапросы требуют, чтобы все поисковые запросы в пакете были независимы, что не всегда так. Иногда запрос B зависит от результатов запроса A, то есть запрос B можно настроить только после выполнения запроса A. Например, вы можете захотеть показывать результаты со вторичного индекса только если в основной таблице не было найдено результатов, либо указать смещение для второго набора результатов на основе количества совпадений в первом наборе. В таких случаях необходимо использовать отдельные запросы (или отдельные пакеты).

При использовании библиотек-коннекторов, таких как `mysqli` в PHP, можно добавить несколько запросов и затем выполнить их как единый пакет. Это будет работать как единый мультизапрос.

Примечание: если вы используете консольный клиент MySQL, по умолчанию он интерпретирует точку с запятой (;) как сам разделитель и отправляет каждый запрос на сервер отдельно; это не мультизапрос. Чтобы изменить такое поведение, переопределите разделитель на клиентской стороне на другой символ с помощью внутренней команды `delimiter`. После этого клиент будет отправлять всю строку с точками с запятой без изменений, что позволит "магии мультизапросов" работать.

Такое поведение консольного клиента часто вызывает путаницу, потому что одна и та же последовательность команд ведет себя по-разному в консоли MySQL по сравнению с другим протоколом, например SQL-over-HTTP. Это происходит именно потому, что консоль MySQL разбивает запросы по точкам с запятой, а другие протоколы могут отправлять всю последовательность как один пакет.

<!-- example multi-query 1 -->
Можно выполнять несколько поисковых запросов в SQL, разделяя их точкой с запятой. Когда Manticore получает такой запрос от клиента, применяются все оптимизации между операторами.

Мультизапросы не поддерживают запросы с `FACET`. Количество мультизапросов в одном пакете не должно превышать [max_batch_queries](../Server_settings/Searchd.md#max_batch_queries).


<!-- intro -->
##### SQL:

<!-- request SQL -->

```sql
SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price DESC; SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price ASC
```
<!-- end -->

Из консольного клиента MySQL/MariaDB:
<!-- request SQL -->

```sql
DELIMITER _
SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price DESC; SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price ASC_
```

<!-- intro -->
##### JSON:

<!-- request JSON -->

```JSON
POST /sql?mode=raw -d "SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price DESC; SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price ASC"
```

<!-- end -->

## Оптимизации мультизапросов

Существуют две основные оптимизации: оптимизация общих запросов и оптимизация общих поддеревьев.

**Оптимизация общих запросов** означает, что `searchd` определит все запросы в пакете, которые отличаются только настройками сортировки и группировки, и *выполнит поиск только один раз*. Например, если пакет состоит из 3 запросов, все они описывают запрос "ipod nano", но первый запрашивает топ-10 результатов, отсортированных по цене, второй группирует по ID поставщика и запрашивает топ-5 поставщиков, отсортированных по рейтингу, а третий запрашивает максимальную цену, полный текстовый поиск по "ipod nano" будет выполнен лишь один раз, а его результаты использованы для построения трёх разных наборов результатов.

[Фасетный поиск](../Searching/Faceted_search.md) — особенно важный случай, который выигрывает от этой оптимизации. Фасетный поиск может быть реализован запуском нескольких запросов: одного для получения собственно результатов поиска и нескольких других с тем же полнотекстовым запросом, но разными настройками группировки, чтобы получить все нужные группы результатов (топ-3 авторов, топ-5 поставщиков и т.д.). Пока полнотекстовый запрос и настройки фильтрации не меняются, срабатывает оптимизация общих запросов, значительно улучшая производительность.

**Оптимизация общих поддеревьев** ещё интереснее. Она позволяет `searchd` использовать сходства между пакетными полнотекстовыми запросами. Ищутся общие части полнотекстовых запросов (поддеревья) во всех запросах и кэшируются между ними. Например, рассмотрим следующий пакет запросов:

```bash
donald trump president
donald trump barack obama john mccain
donald trump speech
```

В этом примере есть общая двусоставная часть `donald trump`, которую можно вычислить только один раз, затем закэшировать и использовать повторно во всех запросах. Именно это и делает оптимизация общих поддеревьев. Размер кэша на запрос строго контролируется директивами [subtree_docs_cache](../Server_settings/Searchd.md#subtree_docs_cache) и [subtree_hits_cache](../Server_settings/Searchd.md#subtree_hits_cache) (чтобы кэширование *всех* миллиардах документов, соответствующих "i am", не исчерпало оперативную память и не вывело сервер из строя).

<!-- example multi-query 2 -->
Как узнать, были ли запросы в пакете действительно оптимизированы? Если были, в соответствующем логе запросов появится поле "multiplier", указывающее, сколько запросов обработано вместе:

Обратите внимание на поле "x3". Это значит, что запрос был оптимизирован и обработан в под-пакете из 3 запросов.


<!-- intro -->
##### лог:

<!-- request log -->
```bash
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/rel 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/ext 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/ext 747541 (0,20)] [lj] the
```
<!-- end -->

<!-- example multi-query 3 -->
Для сравнения, вот как выглядел бы обычный лог, если бы запросы не были объединены в пакет:


<!-- intro -->
##### лог:

<!-- request log -->
```bash
[Sun Jul 12 15:18:17.062 2009] 0.059 sec [ext/0/rel 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.156 2009] 0.091 sec [ext/0/ext 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.250 2009] 0.092 sec [ext/0/ext 747541 (0,20)] [lj] the
```
<!-- end -->

Обратите внимание, что время на обработку отдельного запроса в мультизапросе улучшилось от 1.5x до 2.3x, в зависимости от конкретного режима сортировки.

## Ограничения и особенности работы мультизапросов

Мультизапросы в основном поддерживаются для пакетной передачи запросов и получения мета-информации после таких пакетов. Из-за этого ограничения в пакетах разрешён лишь небольшой набор операторов. В одном пакете можно комбинировать только `SELECT`, `SHOW` и `SET`.

`SELECT` можно использовать как обычно; однако все запросы будут выполняться вместе за один проход. Если запросы не связаны, мультизапрос не даёт преимуществ. Демон это определит и выполнит запросы поодиночке.

Вы можете использовать `SHOW` для обработки *warnings*, *status*, *agent status*, *meta*, *profile* и *plan*. Все остальные операторы `SHOW` в пакетах будут тихо проигнорированы без вывода. Например, вы не можете выполнить `SHOW TABLES`, `SHOW THREADS` или `SHOW VARIABLES`, или любой другой оператор, не упомянутый выше, при пакетной обработке.

Вы можете использовать `SET` только для `SET PROFILING`. Все остальные команды `SET ...` будут тихо проигнорированы.

Порядок выполнения также отличается. Демон обрабатывает пакеты в два прохода.

Сначала он собирает все операторы `SELECT` и одновременно выполняет все операторы `SET PROFILING`, которые видит. Побочным эффектом является то, что эффективным будет только последний оператор `SET PROFILING`. Если вы выполните мультизапрос, например: `SET PROFILING=1; SELECT...; SHOW META; SHOW PROFILE; SET PROFILING=0`, вы не увидите профиль, потому что на первом проходе демон выполняет `SET PROFILING=1`, а затем сразу `SET PROFILING=0`.

Во-вторых, демон пытается выполнить один пакетный запрос со всеми собранными операторами `SELECT`. Если операторы не связаны, он выполнит их по одному.

В конце он проходит по начальному последовательному пакету и возвращает подрезультаты данных и мета из набора результатов для
каждого `SELECT` и `SHOW`. Поскольку все операторы `SET PROFILING` были выполнены в первом проходе, они пропускаются во втором.