# Мультизапросы

Мультизапросы, или пакетные запросы, позволяют отправлять несколько поисковых запросов в Manticore в одном сетевом запросе.

👍 Зачем использовать мультизапросы?

Основная причина — производительность. Отправляя запросы в Manticore пачкой, а не по одному, вы экономите время, уменьшив число сетевых переходов. Кроме того, отправка запросов пачкой позволяет Manticore выполнять определённые внутренние оптимизации. Если оптимизации для пачек неприменимы, запросы будут обработаны индивидуально.

⛔ Когда не стоит использовать мультизапросы?

Мультизапросы требуют, чтобы все поисковые запросы в пачке были независимыми, а это не всегда так. Иногда запрос B зависит от результатов запроса A, то есть запрос B можно сформировать только после выполнения запроса A. Например, вы можете захотеть показать результаты из вторичного индекса только если в основной таблице не найдено результатов, или указать смещение во втором наборе результатов, основываясь на числе совпадений в первом наборе. В таких случаях потребуется использовать отдельные запросы (или отдельные пачки).

При использовании коннекторных библиотек, таких как `mysqli` в PHP, можно добавить несколько запросов и затем выполнить их все как один пакет. Это будет работать как единая мультизапросная пачка.

Примечание: если вы используете консольный MySQL клиент, по умолчанию он воспринимает точку с запятой (;) как разделитель запросов и отправляет каждый запрос на сервер отдельно; это не мультизапросная пачка. Чтобы изменить это поведение, переопределите разделитель на стороне клиента на другой символ с помощью внутренней команды `delimiter`. После внесения этого изменения клиент отправит всю строку с точками с запятой без изменений, позволяя "магии мультизапросов" работать.

Это поведение консольного клиента может вводить в заблуждение, так как одна и та же последовательность команд ведёт себя иначе в консоли клиента MySQL по сравнению с другим протоколом, например, SQL-over-HTTP. Это связано именно с тем, что консольный клиент MySQL разделяет запросы по точкам с запятой, тогда как другие протоколы могут отправлять всю последовательность как единый пакет.

<!-- example multi-query 1 -->
Вы можете выполнять несколько поисковых запросов с SQL, разделяя их точкой с запятой. Когда Manticore получает такой запрос от клиента, применяются все внутренние оптимизации между запросами.

Мультизапросы не поддерживают запросы с `FACET`. Число мультизапросов в одной пачке не должно превышать [max_batch_queries](../Server_settings/Searchd.md#max_batch_queries).


<!-- intro -->
##### SQL:

<!-- request SQL -->

```sql
SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price DESC; SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price ASC
```
<!-- end -->

Из консоли клиента MySQL/MariaDB:
<!-- request SQL -->

```sql
DELIMITER _
SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price DESC; SELECT id, price FROM products WHERE MATCH('remove hair') ORDER BY price ASC_
```
<!-- end -->

## Оптимизации мультизапросов

Существует две основные оптимизации, о которых стоит знать: оптимизация общих запросов и оптимизация общих поддеревьев.

**Оптимизация общих запросов** означает, что `searchd` определит все запросы в пачке, различающиеся только настройками сортировки и группировки, и выполнит поиск *только один раз*. Например, если пачка состоит из 3 запросов, все они по "ipod nano", но первый запрос запрашивает топ-10 результатов, отсортированных по цене, второй группирует по ID поставщика и запрашивает топ-5 поставщиков, отсортированных по рейтингу, а третий запрос — максимальную цену, полнотекстовый поиск по "ipod nano" будет выполнен лишь один раз, а его результаты будут использованы для построения 3 различных наборов результатов.

[Фасетный поиск](../Searching/Faceted_search.md) — особенно важный случай, который выигрывает от этой оптимизации. Действительно, фасетный поиск можно реализовать запуском нескольких запросов: один для получения результатов поиска, и несколько других с тем же полнотекстовым запросом, но разными настройками группировки, чтобы получить все нужные группы результатов (топ-3 авторов, топ-5 поставщиков и т.д.). Пока полнотекстовый запрос и настройки фильтров остаются одинаковыми, срабатывает оптимизация общих запросов, значительно повышая производительность.

**Оптимизация общих поддеревьев** ещё более интересна. Она позволяет `searchd` использовать сходства между пакетными полнотекстовыми запросами. Он выявляет общие части полнотекстовых запросов (поддеревья) во всех запросах и кеширует их между запросами. Например, рассмотрим следующую пачку запросов:

```bash
donald trump president
donald trump barack obama john mccain
donald trump speech
```

Здесь есть общая часть из двух слов `donald trump`, которую можно вычислить один раз, затем закешировать и использовать для всех запросов. И оптимизация общих поддеревьев делает именно это. Размер кеша для каждого запроса строго контролируется директивами [subtree_docs_cache](../Server_settings/Searchd.md#subtree_docs_cache) и [subtree_hits_cache](../Server_settings/Searchd.md#subtree_hits_cache) (чтобы кеширование *всех* бесчисленных документов, подходящих под "i am", не исчерпало оперативную память и не убило сервер мгновенно).

<!-- example multi-query 2 -->
Как понять, были ли запросы в пачке действительно оптимизированы? Если были, соответствующий лог запросов будет содержать поле "multiplier", указывающее, сколько запросов было обработано вместе:

Обратите внимание на поле "x3". Это означает, что данный запрос был оптимизирован и обработан в под-пачке из 3 запросов.


<!-- intro -->
##### log:

<!-- request log -->
```bash
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/rel 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/ext 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.000 2009] 0.040 sec x3 [ext/0/ext 747541 (0,20)] [lj] the
```
<!-- end -->

<!-- example multi-query 3 -->
Для сравнения, вот как выглядел бы обычный лог, если бы запросы не сгруппировали в пачку:


<!-- intro -->
##### log:

<!-- request log -->
```bash
[Sun Jul 12 15:18:17.062 2009] 0.059 sec [ext/0/rel 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.156 2009] 0.091 sec [ext/0/ext 747541 (0,20)] [lj] the
[Sun Jul 12 15:18:17.250 2009] 0.092 sec [ext/0/ext 747541 (0,20)] [lj] the
```
<!-- end -->

Обратите внимание, что время выполнения каждого отдельного запроса в мультизапросе улучшилось в 1.5–2.3 раза, в зависимости от конкретного режима сортировки.

## Ограничения и особенности мультизапросов

Мультизапросы в основном поддерживаются для пакетной отправки запросов и получения метаинформации после таких пакетов. Из-за этого ограничения в пачках разрешён только небольшой поднабор операторов. В одной пачке можно комбинировать только операторы `SELECT`, `SHOW` и `SET`.

Вы можете использовать `SELECT` как обычно; однако все запросы будут выполняться вместе одним проходом. Если запросы не связаны между собой, преимущества мультизапросов отсутствуют. Демон это обнаружит и выполнит запросы по отдельности.

Вы можете использовать `SHOW` для обработки *warnings*, *status*, *agent status*, *meta*, *profile* и *plan*. Все остальные операторы `SHOW` в пакетах будут тихо проигнорированы без вывода. Например, вы не можете выполнить `SHOW TABLES`, `SHOW THREADS` или `SHOW VARIABLES`, или любой другой оператор, не упомянутый выше, при пакетной обработке.

Вы можете использовать `SET` только для `SET PROFILING`. Все остальные команды `SET ...` будут тихо проигнорированы.

Порядок выполнения также отличается. Демон обрабатывает пакеты в два прохода.

Сначала он собирает все операторы `SELECT` и одновременно выполняет все операторы `SET PROFILING`, которые видит. Побочным эффектом является то, что эффективным будет только последний оператор `SET PROFILING`. Если вы выполните мультизапрос, например: `SET PROFILING=1; SELECT...; SHOW META; SHOW PROFILE; SET PROFILING=0`, вы не увидите профиль, потому что на первом проходе демон выполняет `SET PROFILING=1`, а затем сразу `SET PROFILING=0`.

Во-вторых, демон пытается выполнить один пакетный запрос со всеми собранными операторами `SELECT`. Если операторы не связаны, он выполнит их по одному.

В конце он проходит по начальному последовательному пакету и возвращает подрезультаты данных и мета из набора результатов для
каждого `SELECT` и `SHOW`. Поскольку все операторы `SET PROFILING` были выполнены в первом проходе, они пропускаются во втором.