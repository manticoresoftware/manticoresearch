––– comment –––
Test SCHEMA permission enforcement.
Spec: https://github.com/manticoresoftware/manticoresearch/blob/wip_auth/src/auth/spec.md
Issue: https://github.com/manticoresoftware/manticoresearch/issues/2983
Admin credentials: admin / adminpass (bootstrap via auth.json)

Per spec: SCHEMA permission allows CREATE TABLE, ALTER TABLE, DROP TABLE
––– comment –––
––– block: ../base/start-searchd-with-auth –––
––– comment –––
––– Setup: create users –––
––– comment –––
––– input –––
MYSQL_PWD=adminpass mysql -E -uadmin -h0 -P9306 -e "CREATE USER 'schemauser' IDENTIFIED BY 'schemapass1';"
––– output –––
*************************** 1. row ***************************
       token: #!/[0-9a-f]{64}/!#
    username: schemauser
generated_at: #!/([0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}|[A-Z][a-z]{2} [A-Z][a-z]{2} [ 0-9][0-9] [0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3} [0-9]{4})/!#
––– input –––
MYSQL_PWD=adminpass mysql -uadmin -h0 -P9306 -e "GRANT schema ON * TO 'schemauser';"
––– output –––
––– input –––
MYSQL_PWD=adminpass mysql -E -uadmin -h0 -P9306 -e "CREATE USER 'noperm' IDENTIFIED BY 'nopermpass1';"
––– output –––
*************************** 1. row ***************************
       token: #!/[0-9a-f]{64}/!#
    username: noperm
generated_at: #!/([0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}|[A-Z][a-z]{2} [A-Z][a-z]{2} [ 0-9][0-9] [0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3} [0-9]{4})/!#
––– comment –––
––– 4.3.1 CREATE TABLE with SCHEMA — success –––
EXPECTED per spec: Success (GRANT should apply immediately)
BUG: Requires RELOAD AUTH — permissions don't apply immediately
––– comment –––
––– input –––
MYSQL_PWD=schemapass1 mysql -uschemauser -h0 -P9306 -e "CREATE TABLE schematest(content text);"
––– output –––
––– comment –––
––– 4.3.2 CREATE TABLE without SCHEMA — denied –––
EXPECTED per spec: Permission denied
––– comment –––
––– input –––
MYSQL_PWD=nopermpass1 mysql -unoperm -h0 -P9306 -e "CREATE TABLE blocked(content text);" 2>&1
––– output –––
ERROR 1064 (42000) at line 1: Permission denied for user 'noperm'
––– comment –––
––– 4.3.3 ALTER TABLE with SCHEMA — success –––
EXPECTED per spec: Success (GRANT should apply immediately)
BUG: Requires RELOAD AUTH — permissions don't apply immediately
––– comment –––
––– input –––
MYSQL_PWD=schemapass1 mysql -uschemauser -h0 -P9306 -e "ALTER TABLE schematest ADD COLUMN tag integer;"
––– output –––
––– comment –––
––– 4.3.4 DROP TABLE with SCHEMA — success –––
EXPECTED per spec: Success (GRANT should apply immediately)
BUG: Requires RELOAD AUTH — permissions don't apply immediately
––– comment –––
––– input –––
MYSQL_PWD=schemapass1 mysql -uschemauser -h0 -P9306 -e "DROP TABLE schematest;"
––– output –––
