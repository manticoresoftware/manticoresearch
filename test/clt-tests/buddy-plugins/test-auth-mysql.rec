––– comment –––
Test MySQL Protocol authentication (mysql_native_password).
Spec: https://github.com/manticoresoftware/manticoresearch/blob/wip_auth/src/auth/spec.md
Issue: https://github.com/manticoresoftware/manticoresearch/issues/2983
Admin credentials: admin / adminpass (bootstrap via auth.json)

Behavior update: auth failures now return MySQL error code 1045
and preserve whether password was provided.
––– comment –––
––– block: ../base/start-searchd-with-auth –––
––– comment –––
––– 1.1.1 Successful login with correct credentials –––
EXPECTED: Success, returns result
––– comment –––
––– input –––
MYSQL_PWD=adminpass mysql -E -uadmin -h0 -P9306 -e "SELECT 1 AS test;"
––– output –––
*************************** 1. row ***************************
test: 1
––– comment –––
––– 1.1.2 Reject with wrong password –––
EXPECTED: ERROR 1045 (42000): Access denied for user 'admin' (using password: YES)
––– comment –––
––– input –––
MYSQL_PWD=wrongpass mysql -uadmin -h0 -P9306 -e "SELECT 1;" 2>&1
––– output –––
ERROR 1045 (42000): Access denied for user 'admin' (using password: YES)
––– comment –––
––– 1.1.3 Reject with non-existent user –––
EXPECTED: ERROR 1045 (42000): Access denied for user 'nonexistent' (using password: YES)
––– comment –––
––– input –––
MYSQL_PWD=anypass mysql -unonexistent -h0 -P9306 -e "SELECT 1;" 2>&1
––– output –––
ERROR 1045 (42000): Access denied for user 'nonexistent' (using password: YES)
––– comment –––
––– 1.1.4 Reject with empty password –––
EXPECTED: ERROR 1045 (42000): Access denied for user 'admin' (using password: NO)
––– comment –––
––– input –––
mysql -uadmin -h0 -P9306 -e "SELECT 1;" 2>&1
––– output –––
ERROR 1045 (42000): Access denied for user 'admin' (using password: NO)
––– comment –––
––– 1.1.5 Reject shortened mysql_native_password responses –––
EXPECTED: Full 20-byte response succeeds; correct 1, 2, and 19-byte prefixes fail.
––– comment –––
––– input –––
python3 - <<'PY'
import hashlib
import socket
import struct

HOST = "127.0.0.1"
PORT = 9306
USER = "admin"
PASSWORD = "adminpass"

CLIENT_PROTOCOL_41 = 0x00000200
CLIENT_SECURE_CONNECTION = 0x00008000
CLIENT_MULTI_RESULTS = 0x00020000
CLIENT_PLUGIN_AUTH = 0x00080000


def read_packet(sock):
    header = sock.recv(4)
    if len(header) != 4:
        raise RuntimeError("short MySQL packet header")
    length = header[0] | (header[1] << 8) | (header[2] << 16)
    seq = header[3]
    data = bytearray()
    while len(data) < length:
        chunk = sock.recv(length - len(data))
        if not chunk:
            raise RuntimeError("unexpected EOF while reading MySQL packet")
        data.extend(chunk)
    return seq, bytes(data)


def parse_scramble(data):
    pos = 1
    pos = data.index(b"\0", pos) + 1
    pos += 4
    part1 = data[pos:pos + 8]
    pos += 8
    pos += 1
    pos += 2
    pos += 1
    pos += 2
    pos += 2
    auth_len = data[pos]
    pos += 1
    pos += 10
    part2 = data[pos:pos + max(13, auth_len - 8)]
    return (part1 + part2)[:20]


def native_password_token(password, scramble):
    stage1 = hashlib.sha1(password.encode()).digest()
    stage2 = hashlib.sha1(stage1).digest()
    stage3 = hashlib.sha1(scramble + stage2).digest()
    return bytes(a ^ b for a, b in zip(stage3, stage1))


def try_auth(prefix_len=None, mutate_first=False):
    with socket.create_connection((HOST, PORT), timeout=5) as sock:
        _, handshake = read_packet(sock)
        token = native_password_token(PASSWORD, parse_scramble(handshake))
        response = token if prefix_len is None else token[:prefix_len]
        if mutate_first:
            response = bytes([response[0] ^ 0xFF]) + response[1:]

        caps = CLIENT_PROTOCOL_41 | CLIENT_SECURE_CONNECTION | CLIENT_MULTI_RESULTS | CLIENT_PLUGIN_AUTH
        payload = struct.pack("<IIB", caps, 16 * 1024 * 1024, 33)
        payload += bytes(23)
        payload += USER.encode() + b"\0"
        payload += bytes([len(response)]) + response
        payload += b"mysql_native_password\0"
        packet = struct.pack("<I", len(payload))[:3] + b"\x01" + payload
        sock.sendall(packet)
        _, reply = read_packet(sock)

    if reply[0] == 0x00:
        return "OK"
    if reply[0] == 0xFF:
        return "ERR %d" % struct.unpack("<H", reply[1:3])[0]
    return "0x%02x" % reply[0]


cases = [
    ("full_expected", {}),
    ("prefix_1_expected", {"prefix_len": 1}),
    ("prefix_2_expected", {"prefix_len": 2}),
    ("prefix_19_expected", {"prefix_len": 19}),
    ("prefix_1_wrong", {"prefix_len": 1, "mutate_first": True}),
]
for label, kwargs in cases:
    print("%s: %s" % (label, try_auth(**kwargs)))
PY
––– output –––
full_expected: OK
prefix_1_expected: ERR 1045
prefix_2_expected: ERR 1045
prefix_19_expected: ERR 1045
prefix_1_wrong: ERR 1045
