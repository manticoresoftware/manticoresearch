––– block: ../base/start-searchd-with-buddy –––
––– input –––
sleep 3; curl -s localhost:9308/cli -d 'debug sleep 30' &
––– output –––
[%{NUMBER}] %{NUMBER}
––– input –––
sleep 3; curl -s localhost:9308/cli -d 'show queries' | grep -q debug && echo "Successfully" || echo "Unsuccessfully"
––– output –––
Successfully
