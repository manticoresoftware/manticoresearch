––– input –––
mkdir -p /tmp/issue4449-ro/data /tmp/issue4449-ro/run
cat > /tmp/issue4449-ro/docs1.tsv <<'EOF'
1	hello title	plain body one	10
2	second title	plain body two	20
EOF
cat > /tmp/issue4449-ro/docs2.tsv <<'EOF'
11	plain extra	alpha world	110
12	world extra	beta plain	120
EOF
cat > /tmp/issue4449-ro/base.conf <<'EOF'
source src_docs1
{
  type = tsvpipe
  tsvpipe_command = cat /tmp/issue4449-ro/docs1.tsv
  tsvpipe_field = title
  tsvpipe_field = body
  tsvpipe_attr_uint = gid
}
source src_docs2
{
  type = tsvpipe
  tsvpipe_command = cat /tmp/issue4449-ro/docs2.tsv
  tsvpipe_field = title
  tsvpipe_field = body
  tsvpipe_attr_uint = gid
}
index idx_docs
{
  type = plain
  source = src_docs1
  path = /tmp/issue4449-ro/data/idx_docs
}
index idx_docs2
{
  type = plain
  source = src_docs2
  path = /tmp/issue4449-ro/data/idx_docs2
}
searchd
{
  listen = 127.0.0.1:19306:mysql41
  listen = 127.0.0.1:19307:mysql41_readonly
  listen = 127.0.0.1:19308:http
  listen = 127.0.0.1:19312
  log = /tmp/issue4449-ro/run/searchd.log
  query_log = /tmp/issue4449-ro/run/query.log
  pid_file = /tmp/issue4449-ro/run/searchd.pid
  binlog_path = /tmp/issue4449-ro/run
}
EOF
cp /tmp/issue4449-ro/base.conf /tmp/issue4449-ro/ro.conf
python3 - <<'PY'
from pathlib import Path
p=Path('/tmp/issue4449-ro/ro.conf')
p.write_text(p.read_text().replace('searchd\n{','searchd\n{\n  read_only = 1',1))
PY
––– output –––
––– input –––
indexer --config /tmp/issue4449-ro/base.conf --all --quiet
––– output –––
––– input –––
rm -f /tmp/issue4449-ro/run/searchd.log
searchd --config /tmp/issue4449-ro/base.conf > /dev/null
if timeout 10 grep -qm1 'accepting connections' <(tail -n 1000 -f /tmp/issue4449-ro/run/searchd.log); then echo 'Writable daemon started!'; else echo 'Timeout or failed!'; fi
––– output –––
Writable daemon started!
––– input –––
indexer --config /tmp/issue4449-ro/base.conf --rotate --nohup idx_docs idx_docs2 --quiet
indextool --config /tmp/issue4449-ro/base.conf --rotate --check idx_docs > /dev/null
indextool --config /tmp/issue4449-ro/base.conf --rotate --check idx_docs2 > /dev/null
ls /tmp/issue4449-ro/data/idx_docs.new.sph /tmp/issue4449-ro/data/idx_docs2.new.sph >/dev/null && echo pending-new-ready
––– output –––
pending-new-ready
––– input –––
searchd --config /tmp/issue4449-ro/base.conf --stopwait > /dev/null 2>&1 || true
chmod -R a+rX /tmp/issue4449-ro
chmod -R a-w /tmp/issue4449-ro/data
if [ "$(id -u)" = "0" ] && id nobody >/dev/null 2>&1; then
  chown -R nobody /tmp/issue4449-ro/run
fi
echo ready
––– output –––
ready
––– input –––
rm -f /tmp/issue4449-ro/run/searchd.log
if [ "$(id -u)" = "0" ] && id nobody >/dev/null 2>&1; then
  su -s /bin/sh nobody -c "searchd --config /tmp/issue4449-ro/ro.conf" > /dev/null
else
  searchd --config /tmp/issue4449-ro/ro.conf > /dev/null
fi
if timeout 10 grep -qm1 'accepting connections' <(tail -n 1000 -f /tmp/issue4449-ro/run/searchd.log); then echo 'Accepting connections!'; else echo 'Timeout or failed!'; fi
––– output –––
Accepting connections!
––– input –––
grep -F "daemon read-only mode, serving without lock" /tmp/issue4449-ro/run/searchd.log | wc -l
––– output –––
2
––– input –––
grep -F "ignoring pending .new files for table 'idx_docs' in daemon read-only mode" /tmp/issue4449-ro/run/searchd.log | grep -o "ignoring pending .new files for table 'idx_docs' in daemon read-only mode"
––– output –––
ignoring pending .new files for table 'idx_docs' in daemon read-only mode
––– input –––
grep -F "ignoring pending .new files for table 'idx_docs2' in daemon read-only mode" /tmp/issue4449-ro/run/searchd.log | grep -o "ignoring pending .new files for table 'idx_docs2' in daemon read-only mode"
––– output –––
ignoring pending .new files for table 'idx_docs2' in daemon read-only mode
––– input –––
mysql -h127.0.0.1 -P19306 -e "show variables like 'session_read_only';" | awk '/session_read_only/ {print $4}'
––– output –––
1
––– input –––
mysql -h127.0.0.1 -P19307 -e "show variables like 'session_read_only';" | awk '/session_read_only/ {print $4}'
––– output –––
1
––– input –––
curl -s -X POST http://127.0.0.1:19308/search -d '{"index":"idx_docs","query":{"match":{"title":"hello"}}}' | grep -o '"title":"hello title"'
––– output –––
"title":"hello title"
––– input –––
php -r 'require "/usr/share/manticore/api/sphinxapi.php"; $cl=new SphinxClient(); $cl->SetServer("127.0.0.1",19312); $res=$cl->Query("hello","idx_docs"); if ($res!==false && ($res["total"]??0)==1) echo "api_search_ok\n"; else echo "api_search_fail: ".$cl->GetLastError()."\n"; $upd=$cl->UpdateAttributes("idx_docs",["gid"],[1=>[999]]); if ($upd===-1) echo $cl->GetLastError()."\n"; else echo "updated=$upd\n";'
––– output –––
api_search_ok
searchd error: table idx_docs: table is loaded from read-only storage
––– input –––
mysql -h127.0.0.1 -P19306 -e "show tables;" | awk '/idx_docs/ {print $2 "\t" $4}'
––– output –––
idx_docs	local
idx_docs2	local
––– input –––
mysql -h127.0.0.1 -P19306 -e "select count(*) from idx_docs;" | grep -o '[0-9][0-9]*' | tail -1
––– output –––
2
––– input –––
mysql -h127.0.0.1 -P19306 -e "select count(*) from idx_docs2;" | grep -o '[0-9][0-9]*' | tail -1
––– output –––
2
––– input –––
mysql -h127.0.0.1 -P19306 -e "select count(*) from idx_docs where match('hello');" | grep -o '[0-9][0-9]*' | tail -1
––– output –––
1
––– input –––
mysql -h127.0.0.1 -P19306 -e "select count(*) from idx_docs2 where match('world');" | grep -o '[0-9][0-9]*' | tail -1
––– output –––
2
––– input –––
mysql -h127.0.0.1 -P19306 -e "set ro=0; show variables like 'session_read_only';" 2>&1 || true
––– output –––
ERROR 1064 (42000) at line 1: daemon is read-only
––– input –––
mysql -h127.0.0.1 -P19307 -e "show variables like 'session_read_only';" | awk '/session_read_only/ {print $4}'
––– output –––
1
––– input –––
curl -s -X POST http://127.0.0.1:19308/insert -d '{"index":"idx_docs","id":101,"doc":{"title":"x","body":"y","gid":1}}' | grep -o 'connection is read-only'
––– output –––
connection is read-only
––– input –––
mysql -h127.0.0.1 -P19306 -e "create table rt_fail (id bigint, t text);" 2>&1 || true
––– output –––
ERROR 1064 (42000) at line 1: connection is read-only
––– input –––
searchd --config /tmp/issue4449-ro/ro.conf --stopwait > /dev/null 2>&1 || true
rm -rf /tmp/issue4449-ro
echo cleaned
––– output –––
cleaned
