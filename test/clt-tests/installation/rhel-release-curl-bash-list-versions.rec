––– input –––
sed -i s/mirror.centos.org/vault.centos.org/g /etc/yum.repos.d/*.repo; sed -i s/^#.*baseurl=http/baseurl=http/g /etc/yum.repos.d/*.repo
––– output –––
––– input –––
yum install -y -q --skip-broken wget curl which procps diffutils > /dev/null 2>&1; echo $?
––– output –––
0
––– block: installer-url –––
––– input –––
set -o pipefail; (wget -qO- "$MANTICORE_INSTALLER_REPO_URL/bootstrap.sh" | bash -s -- --list-versions-file /tmp/manticore-list-versions.txt) > /tmp/manticore-list-versions.out 2>&1; echo $?
––– output –––
0
––– input –––
rpm -q manticore-repo >/dev/null 2>&1; echo $?
––– output –––
0
––– input –––
grep -E '^([0-9]+:)?[0-9]+[.][0-9]+[.][0-9]+([-_+~.][^[:space:]]+)?$' /tmp/manticore-list-versions.txt | head -n1 >/dev/null; echo $?
––– output –––
0
––– input –––
grep -Ev '^([0-9]+:)?[0-9]+[.][0-9]+[.][0-9]+([-_+~.][^[:space:]]+)?$' /tmp/manticore-list-versions.txt; echo $?
––– output –––
1
––– input –––
cmp -s /tmp/manticore-list-versions.txt <(sort -Vu /tmp/manticore-list-versions.txt); echo $?
––– output –––
0
––– input –––
grep -q 'awk: cmd. line' /tmp/manticore-list-versions.out; echo $?
––– output –––
1
––– input –––
rpm -qa | grep '^manticore' | grep -v '^manticore-repo'
––– output –––
––– input –––
yum remove -y manticore-repo > /dev/null 2>&1; echo $?
––– output –––
0
