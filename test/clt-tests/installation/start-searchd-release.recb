––– input –––
mkdir -p /var/run/manticore
––– output –––
––– input –––
searchd > /dev/null
––– output –––
––– input –––
rm /var/log/manticore/searchd.log; searchd --stopwait > /dev/null; searchd > /dev/null; if timeout 10 grep -qm1 '\[BUDDY\] started' <(tail -n 1000 -f /var/log/manticore/searchd.log); then echo 'Buddy started!'; else echo 'Timeout or failed!'; fi
––– output –––
Buddy started!
