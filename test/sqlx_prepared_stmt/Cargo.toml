[package]
name = "sqlx_prepared_stmt"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8", features = ["mysql", "runtime-tokio"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
